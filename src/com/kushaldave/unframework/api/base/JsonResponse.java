package com.kushaldave.unframework.api.base;

/**
 * Endpoint responses don't need to conform to any interface, but it's helpful to
 * mark them with a type instead of returning Object everywhere.
 */
public interface JsonResponse {
}
